enum Affect {
	Stun;
}

enum States {
	Idle;
	Run;
	JumpUp;
	JumpDown;
}

enum Collectables {
	Heart;
	Diamond;
}

enum LevelMark {
	Grab;
	GrabLeft;
	GrabRight;
	PlatformEnd;
	PlatformEndLeft;
	PlatformEndRight;
}